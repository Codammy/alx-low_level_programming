#include <math.h>

/**
 * _sqrt_recursion - retuens the square root of a number;
 *
 * @n: argument/parameter.
 * Return: sqroot.
 */
int _sqrt_recursion(int n)
{
	int sqroot = sqrt(n);

	return (sqroot);
}
