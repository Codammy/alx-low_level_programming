#include <math.h>

/**
 * _pow_recursion - returns the power of a number;
 *
 * @x: base argument.
 * @y: power argument.
 * Return: power.
 */
int _sqrt_recursion(int x, int y)
{
if (y < 0)
return (-1);
	int power = pow(x, y)

	return (power);
}
