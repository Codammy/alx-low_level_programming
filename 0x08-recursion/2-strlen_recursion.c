#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - prints the length of a string
 * @s: constant pointer
 *
 * Return: len
 */
int _strlen_recursion(char *s)
{
	int len = strlen(s);

	return (len);
}
