#include <ctype.h>
#include "main.h"
/**
 * _isalpha - check for presence of letter
 * @c: retuns 1 if true and 0 if false
 * Return: 0
 */
int _isalpha(int c)
{
	if (isalpha(c))
		return (1);
	else
		return (0);
	return (0);
}
