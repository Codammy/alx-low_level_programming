#include <string.h>

/**
 * _strcat  - concatinates a string pointer to another.
 * @dest: f pointer
 * @src: s pointer
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
