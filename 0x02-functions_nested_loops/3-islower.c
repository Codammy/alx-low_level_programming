#include <ctype.h>
#include "main.h"
/**
 * _islower - check for upper or lower case
 *@c: retuns 1 if true and 0 if false
 * Return: 0
 */
int _islower(int c)
{
	if (islower(c))
		return (1);
	else
		return (0);
	return (0);
}
