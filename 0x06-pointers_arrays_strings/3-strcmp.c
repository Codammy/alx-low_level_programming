#include <string.h>

/**
 * _strcmp  - compares the length of  s1 with s2.
 * @s1: f pointer
 * @s2: s pointer
 * Return: cmp
 */
int _strcmp(char *s1, char *s2)
{
int cmp = strcmp(s1, s2);
return (cmp);
}
